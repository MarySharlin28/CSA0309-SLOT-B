#include<stdio.h>
int main(){
 int a[20],n,i,p,x;
 scanf("%d",&n);
 for(i=0;i<n;i++)scanf("%d",&a[i]);
 scanf("%d%d",&p,&x);
 for(i=n;i>p;i--)a[i]=a[i-1];
 a[p]=x;n++;
 for(i=0;i<n;i++)printf("%d ",a[i]);
 scanf("%d",&p);
 for(i=p;i<n-1;i++)a[i]=a[i+1];
 n--;
 for(i=0;i<n;i++)printf("%d ",a[i]);
}
